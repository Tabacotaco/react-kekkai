import { UUID } from 'types/util';

const LayoutOpts = {
  List: UUID(),
  Form: UUID(),
  Card: UUID()
};

const DropdownColors = {
  bdColor: '#d6d8db',
  bgColor: '#e2e3e5',
  txColor: '#383d41'
};

export { LayoutOpts, DropdownColors };